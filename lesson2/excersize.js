let name = 'Robert Guerra';

let favoriteNumber = 777;

let person = {
  name: 'Robert',
  favoriteNumber: 7,
  eyeColor: 'brown',
  house: {
    address: '777 Lucky St',
    color: 'white',
    zip: 77777,
  },
};

let heights = [60, 50, 40, 80];

function computeCircumference(radius) {
  return Math.pi * radius * radius;
}

console.log(name);
console.log(favoriteNumber);
console.log(person);
console.log(heights);
console.log(computeCircumference(5));
